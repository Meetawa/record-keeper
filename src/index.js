import React from "react";
import ReactDOM from "react-dom/client";
import "./index.css";
import RecordKeeperIndex from "./components/RecordKeeperIndex";
const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <RecordKeeperIndex />
  </React.StrictMode>
);
